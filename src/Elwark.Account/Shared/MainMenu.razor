@using Microsoft.Extensions.Localization

<ul class="menu">
    <li>
        <NavLink href="/" Match="NavLinkMatch.All" ActiveClass="active">
            @L["Profile"]
        </NavLink>
    </li>
    <li>
        <NavLink href="security" Match="NavLinkMatch.Prefix" ActiveClass="active">
            @L["Security"]
        </NavLink>
    </li>
    <li>
        <NavLink href="connections" Match="NavLinkMatch.Prefix" ActiveClass="active">
            @L["Connections"]
        </NavLink>
    </li>
</ul>

@code
{
    [Inject]
    NavigationManager Manager { get; set; } = default!;

    [Inject]
    IStringLocalizer<App> L { get; set; } = default!;
}
